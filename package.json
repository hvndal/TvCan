{
  "name": "tvcan",
  "version": "1.0.0",
  "description": "Desktop live TV streamer for free public IPTV channels",
  "main": "electron/main.js",
  "author": "hvndal",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "electron:dev": "vite & electron .",
    "dist": "vite build && electron-packager . tvcan --platform=win32 --arch=x64 --out=dist-app --overwrite"
  },
  "dependencies": {
    "hls.js": "^1.5.20"
  },
  "devDependencies": {
    "electron": "^34.2.0",
    "electron-packager": "^17.1.2",
    "vite": "^6.1.0"
  }
}
